"""PubMed GWAS baseline ranking (scripts/pubmed_baseline_gwas.py).

Merged CSVs and ``run_meta.json`` under ``ranking/output/pubmed_baseline_gwas/`` are meant
to be committed for docs builds. Per-baseline ``*.shard.json`` files stay under ``shards/``
(gitignored). Refresh Trending with ``scripts/render_trending_pubmed_gwas.py``.
"""
